# Football Betting Agent - Requirements
# Python 3.10+

# Scraping
selenium>=4.15.0
undetected-chromedriver>=3.5.0
beautifulsoup4>=4.12.0
lxml>=5.0.0
requests>=2.31.0
aiohttp>=3.9.0
httpx>=0.25.0
playwright>=1.40.0
camoufox>=0.4.0        # Firefox-based anti-fingerprinting browser (Cloudflare bypass)

# Database
#
# UPPER BOUND, 2026-09-25 — REMOVE ONLY DELIBERATELY.
# SQLAlchemy 2.1.0 changed the DEFAULT DBAPI for a bare `postgresql://` URL from
# psycopg2 to psycopg (v3). This repo ships psycopg2-binary and does NOT ship
# psycopg, so on 2.1.0 every `create_engine` against Neon raises
# `ModuleNotFoundError: No module named 'psycopg'` inside
# sqlalchemy/dialects/postgresql/psycopg.py.
#
# It released between 15:48 and 20:28 UTC on 2026-09-24 and took closing-lines
# down on the 20:28 and 23:51 runs with no change in this repository — the 15:48
# run succeeded on 2.0.54. `Verify database connection` carries no
# continue-on-error, so it also halts daily-picks before its tests and skips
# every core step.
#
# TO LIFT IT: add `psycopg[binary]` (v3) OR write the driver explicitly into
# DATABASE_URL as `postgresql+psycopg2://`, then verify against a real Postgres
# URL — not SQLite, which never reaches this code path.
sqlalchemy>=2.0.0,<2.1
psycopg2-binary>=2.9.0
alembic>=1.13.0

# Data Processing
pandas>=2.1.0
# Parquet engine for the local match-history mirror (src/data/history_mirror.py).
# Optional at runtime: without it the mirror is skipped and history is read from
# the database, which costs egress but nothing else.
pyarrow>=15.0.0
numpy>=1.26.0

# Machine Learning
scikit-learn>=1.3.0
xgboost>=2.0.0
lightgbm>=4.1.0
# tensorflow>=2.15.0  # optional, for neural network

# Statistics
scipy>=1.11.0
statsmodels>=0.14.0

# NLP (for news sentiment)
transformers>=4.35.0
nltk>=3.8.0
vaderSentiment>=3.3.2

# Scheduling
apscheduler>=3.10.0

# Notifications
python-telegram-bot>=20.0

# LLM match briefings (Claude API with server-side web search)
anthropic>=0.69.0

# Utilities
pyyaml>=6.0.0
python-dotenv>=1.0.0
loguru>=0.7.0

# Testing
pytest>=7.4.0
pytest-asyncio>=0.21.0
